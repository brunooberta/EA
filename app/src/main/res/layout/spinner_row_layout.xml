<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/lay_spinner_row"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/black"
    android:orientation="horizontal"
    android:padding="3dp">

    <ImageView
        android:id="@+id/imgThumb"
        android:layout_width="@dimen/spinner_dimension_icon"
        android:layout_height="@dimen/spinner_dimension_icon"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="20dp"
        android:layout_marginTop="5dp"
        android:src="@mipmap/flag_map_marker_blue" />

    <TextView
        android:id="@+id/description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="5dp"
        android:layout_marginTop="5dp"
        android:elevation="0dp"
        android:text="DESCRIPTION"
        android:textAllCaps="true"
        android:textColor="@color/calendar_background"
        android:textSize="18sp" />


</LinearLayout>